/**
 * User service to get user data from the Redux store
 * 
 * @deprecated This file is kept for backward compatibility.
 * Please import directly from '../helpers/user.helper' instead.
 */

// Re-export from helpers for backward compatibility
export { getCurrentUser, getCurrentUserId } from '../helpers/user.helper';
